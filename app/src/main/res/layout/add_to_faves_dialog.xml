<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:background="#ffffffff"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/heart"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="14dp"
        android:gravity="center"
        android:scaleType="fitCenter"
        android:src="@drawable/like" />

    <TextView
        android:id="@+id/addtoFaves_text_dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/heart"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="14dp"
        android:gravity="center_horizontal"
        android:text=" Give this road a name"
        android:textColor="#ff000000"
        android:textSize="18sp"
        tools:text="@string/addtoFaves_text_dialog" />

    <EditText
        android:id="@+id/road_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/addtoFaves_text_dialog"
        android:layout_centerHorizontal="true"
        android:hint="e.g. paris chill"
        android:inputType="text"
        android:textColor="#ff000000"
        android:textColorHint="#ff000000" />

    <Button
        android:id="@+id/btn_dialog"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="15dp"
        android:gravity="center_vertical|center_horizontal"
        android:text="ADD TO FAVORITES"
        android:textColor="#ffffffff" />

</RelativeLayout>